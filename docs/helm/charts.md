# Helm Charts

Helm packages Kubernetes manifests into charts. A chart contains templates, default values, metadata, and optional dependencies.

The `values.yaml` file provides default configuration. Users can override values during installation or upgrade with custom values files or command-line flags.

Helm is commonly used to standardize deployments and repeat environment-specific configuration.
