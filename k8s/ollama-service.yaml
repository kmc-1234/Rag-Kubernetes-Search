apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: rag-kubernetes-search
  labels:
    app: ollama
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - name: http
      port: 11434
      targetPort: http
